import { style } from '@vanilla-extract/css';

export const off_div = style({
  display: 'flex',
  flexDirection: 'column',
  height: '40vh',
});
export const off_alien = style({
  display: 'flex',
  marginTop: 'auto',
  marginBottom: '2rem',
  alignSelf: 'center',
});
export const off_g = style({
  textAlign: 'center',
});

export const off_container = style({
  marginBottom: '1.5rem',
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
});

export const off_mt = style({
  marginTop: '.5rem',
});
